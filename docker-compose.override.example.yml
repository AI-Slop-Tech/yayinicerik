# Proxy'siz sunucu için: nginx'i doğrudan 80 portundan dışarı açar.
# Kullanım: cp docker-compose.override.example.yml docker-compose.override.yml
services:
  nginx:
    ports:
      - "${HTTP_PORT:-80}:80"
